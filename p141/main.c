#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
	long long int cc = 0;

	int lim = 1000000;
	for(int a=1;a<lim;++a){
		for(int q=1;q*q<lim;++q){
			//cc++;
			double ad = a, qd = d, sq, err = 1.0;
			sq = sqrt(ad*ad*ad*ad-4.0*qd*qd*qd);
			err = 
			if 
		}
	}

	printf("%ld\n",cc);

	return 0;
}

